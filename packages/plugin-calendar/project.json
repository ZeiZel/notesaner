{
  "name": "plugin-calendar",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/plugin-calendar/src",
  "projectType": "library",
  "tags": ["scope:shared", "type:plugin"],
  "targets": {
    "type-check": {
      "command": "tsc --noEmit -p packages/plugin-calendar/tsconfig.json"
    },
    "test": {
      "command": "vitest run",
      "options": {
        "cwd": "packages/plugin-calendar"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["eslint {projectRoot}/src --ext .ts,.tsx"]
      }
    }
  }
}
