{
  "name": "plugin-templates",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/plugin-templates/src",
  "projectType": "library",
  "tags": ["scope:shared"],
  "targets": {
    "type-check": {
      "command": "tsc --noEmit -p packages/plugin-templates/tsconfig.json"
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm vitest run",
        "cwd": "packages/plugin-templates"
      }
    }
  }
}
