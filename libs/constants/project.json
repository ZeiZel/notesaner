{
  "name": "constants",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/constants/src",
  "projectType": "library",
  "tags": ["scope:lib", "type:lib"],
  "targets": {
    "type-check": {
      "command": "tsc --noEmit -p libs/constants/tsconfig.json"
    },
    "test": {
      "command": "vitest run --config libs/constants/vitest.config.ts"
    },
    "test:watch": {
      "command": "vitest --config libs/constants/vitest.config.ts"
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["eslint {projectRoot}/src --ext .ts,.tsx"]
      }
    }
  }
}
