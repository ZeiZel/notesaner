/**
 * Entities layer — domain entities and their representations.
 *
 * This layer will be populated in Phase 3 when features are decomposed
 * into entities (Note, Workspace, User, Plugin, etc.).
 *
 * For now, this is an empty placeholder to establish the FSD layer.
 */
