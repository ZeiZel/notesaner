/** BullMQ queue name for image optimization jobs. */
export const IMAGE_OPTIMIZE_QUEUE = 'image-optimize';

/** Job name for optimizing a single attachment image. */
export const OPTIMIZE_IMAGE_JOB = 'optimize-image';

/** Maximum number of concurrent image optimization workers. */
export const IMAGE_OPTIMIZE_CONCURRENCY = 2;
